import React from "react";
import { SearchBar } from "../components";

export const SearchScreen = () => {
    return (
        <section className="w-full flex flex-col justify-start items-start px-4">
            <SearchBar />
        </section>
    );
};
